app = "cyborg-pool-warmer"
primary_region = "lhr"

[build]

[[vm]]
size = "shared-cpu-1x"
memory = "512mb"

[deploy]
strategy = "immediate"

# Single always-on machine; no http_service (the warmer is internal-only).
[processes]
warmer = "python warmer.py"
