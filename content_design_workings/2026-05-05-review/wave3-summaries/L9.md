# W3-L9 summary

**Module written:** `course/managed-agents-memory-advisor.md` (placeholder name; Wave 4 renumbers to Layer 9, slotting between L7 in-CLI subagent design and L10 production architecture).

**Thesis.** Three hosted/cross-session agent primitives that live outside the in-CLI subagent model — Claude Managed Agents, Memory, and the Advisor pairing pattern — each addressing a distinct failure mode (where the agent runs, what it remembers, who keeps it on-thread). The eight-layer course spine teaches prompting and orchestration of one model in one session; L9 teaches substrate selection for agents that escape both frames.

**Shape (eight sections, ~310 lines).** Mission paragraph; three-primitive overview table; deeper treatments of Managed Agents (sandbox, built-in tools, SSE, audit, when to prefer over in-CLI), Memory (file-backed, scoped, audited, exportable, the authorship-inversion vs CLAUDE.md), and Advisor (executor+advisor cost shape, composition with structured outputs and `xhigh`); a three-question decision tree (in-CLI vs Managed vs Advisor) flagged as the load-bearing teaching; worked example using a year of GitHub issues across the three implementations, concluding the production answer is Managed Agent + Memory + Advisor; composition section pointing back to L3/L5/L6/L6.5/L7 and forward to L10; references.

**Conventions honoured.** Frontmatter matches `skills-plugins-hooks.md`. Every beta reference is date-stamped "(public beta as of 2026-05-05)" with the relevant header (`managed-agents-2026-04-01` for Managed Agents + Memory; `advisor-tool-2026-03-01` for Advisor). No fabricated source paths — links go to anthropic.com/news, platform.claude.com/docs, anthropics/claude-agent-sdk-python, and sibling course modules.

**Coordination flags logged in `wave3-notes/L9.md`.** L7 should cross-link back to the §5 decision tree rather than re-derive it; L10 should reference §3 (Memory) and §7 (composition) when covering cross-session state and enterprise residency. Plan §6 open decision #2 (new L9 vs expand L7) is being executed as new-L9; that's a deliberate divergence from the plan's own recommendation, noted for Wave 4.

**Skipped (with reason).** `ant` CLI tutorial depth (out-of-scope per Plan §7); per-agent `mcpServers` and sub-agent prompt-cache sharing (belong in L7); Compaction API depth (L4); production primitives like residency and rate limits (L10).

**Commits.** Attempted but Bash `git` was denied in this sandbox — see report.
