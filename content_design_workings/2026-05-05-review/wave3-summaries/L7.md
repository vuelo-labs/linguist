# Wave 3 — L7 (Agent Design) summary

_Authored 2026-05-05._

## Edits applied
- Added `course-revision: 2026-05-05` frontmatter (matches L1–L6).
- Rewrote opening to scope L7 to **in-CLI subagent design only**, with an explicit forward pointer to the parallel module `managed-agents-memory-advisor.md` for Managed Agents, Memory, and Advisor.
- Kept Skills 16 (Delegation Decision) and 17 (Briefing) intact; their substance was sound. Applied the cc1-audit provenance hedge to the `tools/AgentTool/prompt.ts` quotes ("internal module path inside shipped npm bundle, corroborated by reverse-engineering write-ups, exact wording may have drifted").
- Added new section **Subagent cost shape** covering F07-3 in full: `CLAUDE_CODE_FORK_SUBAGENT=1` semantics (v2.1.117/121), sub-agent prompt-cache sharing ~3× `cache_creation` reduction (v2.1.128), per-agent MCP servers via frontmatter `mcpServers` (v2.1.117). All version-stamped and date-stamped.
- Added "Where this layer ends" closing section with three explicit forward pointers (Managed Agents / Memory / Advisor) into the sibling module.
- Updated the "Next" forward-pointer to acknowledge L8's production-primitives reframe and the parallel hosted-substrate module.

## Scope changes vs. plan
The original Wave-3 plan (05-full-sweep-plan §2) folded Managed Agents, Memory, and Advisor *into* L7 (Blocker B07-1, NL-2 inline). User redirected mid-stream: those primitives now live in a standalone parallel module. L7 is in-CLI-only with hand-offs. F07-2 (Advisor) reduced to a forward pointer; B07-1 deferred to the new module.

## Hand-offs to the new module
Three named forward pointers in "Where this layer ends" (hosted Managed Agents, Memory, Advisor pattern). Decision rule stated: stay in L7 when the work fits one CLI session as a delegated sub-loop; cross over when the substrate is hosted, persistent, or paired.

## Flagged but not applied
- Forward refs in `skills-plugins-hooks.md:181` and `04-context-awareness.md` still promise L7 covers Managed Agents/Advisor — out of scope; logged for the Wave-4 index/coherence pass.
- Renumbering (L7 vs current numbering) — Wave 4.
- F07-4 ("Beyond Skill 18" coda) lives in 08, not 07 — L8's task.
- Bash/git was denied throughout, so no checkpoint commits were made; user will need to commit from the working tree.
