# L10 (08-production-architecture) running notes

_Started 2026-05-05. Append-only log of decisions, oddities, things kept vs. discarded, follow-ups._

## Plan

Full rewrite per Wave-3 plan §2 / §6. Structure:

1. Frontmatter `course-revision: 2026-05-05`.
2. Module intro: production primitives in 2026; the new discipline is cache-bust hygiene + production controls + model selection.
3. **Skill 18 — Cache-bust hygiene under automatic prompt caching.** New thesis. Three worked examples (coding / data / content) rebuilt under auto-caching math.
4. **Sidebar: "What we used to teach: the dynamic boundary."** Preserves the old `SYSTEM_PROMPT_DYNAMIC_BOUNDARY` / `splitSysPromptPrefix` framing as Claude-Code-internal context, with verified citations.
5. **Production primitives** — `inference_geo` (data residency), Rate Limits API, Models API capabilities, Fast mode, Compaction API.
6. **Model selection in 2026** — Opus 4.7 / Sonnet 4.6 / Haiku 4.5; retirement timeline.
7. **Beyond Skill 18 — what's next.** Rewrite of the old "what comes next" coda: agent swarms / stateful sessions / OTEL instrumentation are now shipped (forward-link to managed-agents-memory-advisor.md).
8. Updated source-grounding footer (preserves CC-1 hedge).

## Section checkpoints (commit boundaries)

- [ ] C1: Frontmatter + intro + thesis paragraph
- [ ] C2: Skill 18 rewrite (cache-bust hygiene + 3 examples)
- [ ] C3: Historical sidebar (old thesis preserved)
- [ ] C4: Production primitives section
- [ ] C5: Model selection + retirement timeline
- [ ] C6: Beyond Skill 18 + footer

## Discarded vs. kept from old module

**Kept:**
- The three-domain example structure (coding / data / content). Useful pedagogically.
- The `SYSTEM_PROMPT_DYNAMIC_BOUNDARY` / `splitSysPromptPrefix` / `cacheScope` / `constants/prompts.ts` / `utils/api.ts` / `utils/claudemd.ts` citations — preserved in the historical sidebar with CC-1 provenance hedge.
- The "tool descriptions must remain stable" insight — now reframed as MCP `alwaysLoad` discipline.
- The `promptCacheBreakDetection.ts` reference — now used as inspiration for the OTEL/instrumentation paragraph.

**Discarded:**
- The old thesis statement that manual boundary placement is the production discipline. Auto-caching (Feb 19, 2026) made this a Claude-Code-internal curiosity, not a builder's API task.
- The old "Beyond Skill 18" coda's framing of agent swarms / stateful sessions / instrumentation as "topics that build on this course." All three have shipped as Anthropic primitives in 2026; reframed as forward-links.
- The `cacheScope: 'global'` / `splitSysPromptPrefix` worked-example math — replaced by auto-caching cache-hit / cache-miss math.

## Cross-refs to verify (during write)

- Forward: `managed-agents-memory-advisor.md` (L9, parallel agent).
- Back: L4 (Compaction API decision tree), L5 (xhigh / effort / structured outputs / per-task budgets), L6 (tool taxonomy + MCP `alwaysLoad`).

## Coordination flags for L7 / L9

- L9 should reference L10's "Production primitives" section for `inference_geo`, Rate Limits API, Models API. Likely already planned.
- L10's "Beyond Skill 18" forward-links to L9 — verify L9 owns the Memory + Advisor + Managed Agents deep dive. (Per plan, yes.)

## Commit status

Granular commits were planned at section checkpoints (C1-C6) but `git add` and `git commit` are denied in this sandbox. The full rewrite is on disk in a single working-tree change; the orchestrator/user will need to commit it. Suggested message prefix per convention: `W3-L10:`. A single commit covering the full rewrite is acceptable here given the constraint, though the spec called for granular ones.

## Observations

- Old module says "fork semantics in Claude Code (where a fork inherits parent context and shares the cache) are a starting point" — the v2.1.128 release notes confirm sub-agent prompt-cache sharing now gives ~3× cache_creation reduction; that's now a shipped quantified claim, not a "starting point". Updating.
- `promptCacheBreakDetection.ts` path is in CC-1 audit as "not directly confirmed; used as inspiration only." Will keep that hedge if I cite it; otherwise drop.
- Auto-caching is "single `cache_control` field; system auto-advances cache point as conversation grows" (api-and-services.md) — the production builder's job is to *not* break the prefix, which means: stable system prompt, stable tool list, stable MCP server set, summarised reconnects (v2.1.128 ships this for free).
- Opus 4.7 specifics worth pulling: `xhigh`, 2576px vision, per-task token budgets. Sonnet 4.6: 1M GA + agentic search. Haiku 4.5 mentioned (announce date not in research file but it's in the model lineup).

## Cross-ref / coordination flags for L7 and L9 (post-rewrite)

For the L9 (managed-agents-memory-advisor.md) author: L10's "Beyond Skill 18" forward-references your module by that filename. The framing I used:

- Managed Agents = hosted substrate for what used to be agent-swarm hand-rolling. Beta header `managed-agents-2026-04-01`, dated 2026-04-08.
- Memory = file-backed cross-session state with scoped permissions and audit logs. Dated 2026-04-23.
- Advisor pattern = executor/advisor pairing. Beta header `advisor-tool-2026-03-01`, dated 2026-04-09.

If your module uses different verbs (e.g. "executor + reviewer" instead of "executor + advisor"), flag it back so I can adjust the L10 paragraph.

For the L7 (in-CLI subagents) author: L10's tool-list-stability discipline (MCP `alwaysLoad`, reconnect-summarisation) is in Skill 18; subagent prompt-cache sharing (~3× cache_creation reduction, v2.1.128) is mentioned only in passing in the "Beyond Skill 18" coda. If you cover sub-agent cache sharing in detail in L7, L10 doesn't need to repeat it; but a back-reference from L7's cache-shape section to L10's Skill 18 (cache-bust hygiene from the *outer* prefix's perspective) would land well.

## Things to keep an eye on (follow-ups)

- The Haiku 4.5 announcement date isn't in the api-and-services.md research file. I described it as "the fast/cheap tier" without a specific announce date. If a date surfaces in the next research pass, drop it into the model lineup paragraph.
- The Compaction API beta status as of 2026-05-05 is still "beta" — Layer 4 already date-stamps this. L10 inherits the stamp. Re-verify on the next sweep.
- The Rate Limits API doesn't have a beta header in the research notes (it's listed as a plain API feature dated 2026-04-24). I described it as production-available rather than beta. Confirm.
- `promptCacheBreakDetection.ts` is in the CC-1 audit as "not directly confirmed; used as inspiration only." I dropped the explicit citation from the body of L10 (kept only in the grounding footer with the same hedge). Old L10 line 177 cited it inline as "a model for this" instrumentation — that line is gone. The footer mention preserves the path for any future audit.
