# Wave 3 — L7 (Agent Design) running notes

_Live log. Append as work progresses. 2026-05-05._

## Scope clarification (received from user)
Original Wave-3 plan put Managed Agents + Memory + Advisor inside L7. User has carved those into a **separate parallel module** (`course/managed-agents-memory-advisor.md`). L7 is now in-CLI subagent design only, with forward pointers to that module wherever the narrative bumps into hosted / Memory / Advisor territory.

## Inputs read
- 01-freshness-audit (L7 section): Blocker B07-1 (Managed Agents/Memory/Advisor missing) — now handed off to new module. Updates F07-2..F07-5 still apply or partially apply.
- 05-full-sweep-plan §2 (07-agent-design): Old plan said "extend L7 with Managed Agents" — now overridden by user.
- research-updates/2026-05-05/claude-code-and-skills.md: confirms `CLAUDE_CODE_FORK_SUBAGENT=1` (v2.1.117/121), agent frontmatter `mcpServers` (v2.1.117), sub-agent prompt-cache sharing ~3× cache_creation reduction (v2.1.128), idle-subagent summaries fix (v2.1.128), Advisor tool experimental (v2.1.117).
- wave2.5/cc1-source-path-audit.md: `tools/AgentTool/prompt.ts` is plausible-not-confirmed; the convention is established but path not directly re-quoted in this audit. Recommended language: "internal module paths inside the shipped npm bundle, not files in the public repo; corroborated by reverse-engineering write-ups; exact wording may have drifted".
- wave2.5/coherence-readout.md: forward refs from L4 / L6.5 currently land at "L7 (Agent Design) goes one level further again — designing longer-horizon agents that span in-CLI subagents, hosted Managed Agents, and the Advisor pairing pattern." With user's scope shift, those forward refs should now point partly at L7 (in-CLI) and partly at the new module (hosted/Memory/Advisor). I am NOT touching L4/L6.5 in this task — but flagging it for the index/coherence pass.

## Decisions
- Keep the existing two-skill spine (Skill 16: Delegation Decision; Skill 17: Briefing). The audit didn't say it was wrong, only that L7 was missing the surrounding 2026 surface. The skills themselves are sound.
- Add a new sub-section **Subagent cost shape** between Skill 16 and Skill 17 (or after 17 — see below) to land F07-3 (fork semantics, prompt-cache sharing, per-agent `mcpServers`). This is L7-internal; doesn't poach the new module's territory.
- Add a closing **"Where this layer ends"** section that names `managed-agents-memory-advisor.md` explicitly and forward-points for hosted/Memory/Advisor.
- Replace the old "Beyond Skill 18" coda framing — but that footer actually lives in 08, not 07. Audit cross-listed F07-4 to 08. So in L7 specifically, the coda I update is the closing section pointing forward to L8. I will frame it as: "L8 is next (production primitives). The hosted-substrate sibling module is `managed-agents-memory-advisor.md`."
- Source-path provenance: apply the cc1-audit-recommended hedge consistent with the new 00-index banner. Use language like "internal module path in the shipped npm bundle (v2.1.128, 2026-05-05); corroborated by independent reverse-engineering write-ups; exact wording may have drifted". Apply once on first occurrence, then refer back.
- Frontmatter: add `course-revision: 2026-05-05` to match L1–L6.
- Voice target: L4–L6 register. Imperative, mechanical. No filler. Concrete.

## Things flagged but NOT applied (and why)
- Renumbering / re-spining (skills-plugins-hooks → L7) — out of scope; Wave 4.
- Fixing forward refs in skills-plugins-hooks.md and 04-context-awareness.md that promise L7 covers Managed Agents/Advisor — out of scope for L7 task; will need adjustment when the new module lands and 00-index is rewritten. Logged for the index pass.
- Editing 08-production-architecture.md "Beyond Skill 18" coda (F07-4 cross-listed to 08) — that's L8's task. I'm only updating L7's *forward pointer to L8*.
- Adding worked examples for sub-agent prompt-cache sharing — the existing three examples (per skill) cover the design decisions. Adding a cache-shape example would balloon the module past peer-module length. The cost-shape sub-section names the mechanism with the version stamp; that's enough at this layer.

## Observations / oddities
- Existing L7 voice is already pretty close to L4–L6 target. Less rework on register than I expected.
- Skill 16 cites a direct quote from `tools/AgentTool/prompt.ts` ("Fork yourself when the intermediate tool output isn't worth keeping in your context"). cc1-audit notes this exact wording isn't re-confirmed v2.1.128. Hedging language applied; quote retained because the substantive claim — that delegation is for "tool output not worth keeping" — is the canonical framing in every reverse-engineering write-up.
- Skill 17 cites two quotes ("Brief the agent like a smart colleague…", "Never delegate understanding…"). Same hedge applies.
- The audit's "Beyond Skill 18" footer reference is to L8, not L7. L7 has no such footer in the existing file. So F07-4 in L7 reduces to: update the forward pointer to L8 to acknowledge that L8 itself has been rewritten around production primitives (per the same Wave-3 plan), and acknowledge the parallel hosted-substrate module.

## Work log
- 2026-05-05: read inputs, drafted plan above, about to start writing.
- 2026-05-05: rewrote 07-agent-design.md in place. Added frontmatter, scope-shifted opening (in-CLI only; explicit handoff to managed-agents-memory-advisor.md), kept Skill 16 + Skill 17 with hedged provenance on the AgentTool quotes, added new "Subagent cost shape" section (fork / cache sharing / per-agent mcpServers, all v2.1.117–v2.1.128 stamped), added "Where this layer ends" closing with three forward pointers to the new module, updated the "Next" forward-pointer to L8 to acknowledge L8's production-primitives reframe.
- 2026-05-05: bash/git tool denied repeatedly — could not commit checkpoints as the standing convention requires. Flagging for the user; left the file in a coherent saved state. The whole rewrite is one logical commit anyway.

## Post-write follow-ups for the index/Wave-4 pass
- skills-plugins-hooks.md:181 currently says "L7 ... designs longer-horizon agents that span in-CLI subagents, hosted Managed Agents, and the Advisor pairing pattern." After this rewrite, L7 only does the first; the latter two live in managed-agents-memory-advisor.md. Needs updating in the Wave-4 coherence sweep.
- 04-context-awareness.md and similar should forward-point to managed-agents-memory-advisor.md (for Memory) rather than L7 once that module lands.
- 00-index.md: when rewritten in Wave 4, the L7 row should say "in-CLI subagent design" and a sibling row (or sidebar) should name managed-agents-memory-advisor.md as a parallel module.
- 08-production-architecture.md: the "Beyond Skill 18" coda (audit F07-4, cross-listed to 08) is L8's task; not touched here. L8's rewrite should acknowledge that agent-swarms-as-future is now (a) shipped in-CLI via the parallel-agent plugins (`code-review`, `pr-review-toolkit`) and (b) shipped hosted via Managed Agents.
