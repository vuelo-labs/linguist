# W3-L9 running notes

_Module: managed-agents-memory-advisor.md (placeholder; will be renumbered as Layer 9 in Wave 4)._

## Inputs read
- `content_design_workings/2026-05-05-review/05-full-sweep-plan.md` — §3 NL-2, §2 L7 row, Wave 3 framing.
- `research-updates/2026-05-05/api-and-services.md` — beta dates and headers (Managed Agents Apr 8, Memory Apr 23, Advisor Apr 9; headers `managed-agents-2026-04-01`, `advisor-tool-2026-03-01`).
- `research-updates/2026-05-05/capabilities-and-research.md` — Opus 4.7 + `xhigh`, Sonnet 4.6/Haiku 4.5 tiering relevant to executor/advisor pairing.
- `course/03-session-hygiene.md` — already establishes Memory at the session-hygiene layer (the three-surface table). L9 inherits and deepens that framing rather than re-litigating it.
- `course/skills-plugins-hooks.md` — voice/format precedent for a parallel "standalone module" file with the same frontmatter (`status: standalone module (Wave 4 will renumber)`).

## Decisions
- **Plan §6 open decision #2 ("new L9 vs expand L7") is being executed as the new-L9 path.** That contradicts the plan's own _recommendation_ (extend L7) but matches the user's instruction in the prompt. Logged here so the Wave-4 reviewer doesn't think the contradiction was missed.
- Frontmatter mirrors `skills-plugins-hooks.md` exactly: `course-revision: 2026-05-05` + `status: standalone module (Wave 4 will renumber)`.
- Title in body: "Layer 9 — Managed Agents, Memory, and the Advisor Pattern" with a parenthetical noting the placeholder ordering. This matches L6.5's own self-numbering convention.
- Worked example: a year of GitHub issues, classified, weekly digest. Picked because it's long-horizon (Memory-relevant), audit-relevant (enterprise framing), and has a natural strategic-vs-tactical split (Advisor-relevant).
- Date-stamp policy: every beta reference carries "(public beta as of 2026-05-05)" plus the beta-header string. Matches L3's Memory section and skills-plugins-hooks.md's `as of v2.x.y` cadence.

## Things skipped (with reason)
- **`ant` CLI deep dive.** Plan §7 ("Out of scope: no `ant` CLI tutorial content beyond a sidebar in claude-code-best-practices-deep") explicitly says no tutorial here. L9 mentions `ant` once as the day-to-day surface for Managed Agents and points at claude-code-best-practices-deep.
- **Per-agent `mcpServers` and sub-agent prompt-cache sharing.** The plan puts these in L7 (in-CLI subagent design), not in L9. Mentioned only as a bridge-back-to-L7 cross-reference.
- **Compaction API depth.** Lives in L4 per plan §2 / F04-2. L9 only references it as a complement to Memory.
- **Production primitives (residency, rate limits, models capabilities).** Live in L8/L10. L9 forwards to them.

## Coordination items for L7 / L10
- **For W3-L7:** L9's decision tree explicitly references "in-CLI subagents" as a peer of Managed Agents. L7 should cross-link _back_ to this module's decision tree (the "in-CLI vs Managed vs Advisor" section) rather than re-deriving it. Suggested anchor: section 6 of this module.
- **For W3-L10:** Memory + Compaction API + per-agent observability all bridge into production architecture. L10 should reference §4 (Memory) and §8 (composition) of this module when discussing cross-session state at scale. Audit-log + export-API framing in particular is load-bearing for any enterprise-residency story L10 builds.
- **Possible drift risk:** if L7 redefines "subagent" terminology (e.g. drops "in-CLI" qualifier), the decision tree here will read awkwardly. Worth a Wave-4 pass to align vocabulary across L7/L9/L10.

## Surprises / oddities
- The Plan recommends folding Managed Agents into L7 as NL-2; the user is now spinning it out as L9 instead. That's the correct call for an Advanced learner — the substrate shift is large enough that interleaving it with in-CLI subagent design would muddle both — but it does mean L7's outline shrinks a little.
- Memory's "model writes; human reviews via audit log" inversion of CLAUDE.md is the single most important framing in this module. L3 introduces it; L9 makes it the load-bearing distinction. Worth checking the L3 section keeps that framing crisp after L9 lands.
- Advisor + Opus 4.7 `xhigh` is a natural pairing the research note hints at but doesn't spell out. L9 makes the connection explicit (executor=Haiku/Sonnet, advisor=Opus 4.7 xhigh).

## Follow-ups
- After L7 + L10 land, verify cross-references resolve.
- Confirm the `managed-agents-2026-04-01` header still names both Managed Agents _and_ Memory (per `api-and-services.md` it does — same header). If that splits in a future spec move, L9 will need patching.
- `advisor-tool-2026-03-01` header date predates the Apr 9 GA-as-beta announcement in the research note. That's normal (header strings are spec-version stamps, not announcement dates), but worth a footnote if a reader queries it.
