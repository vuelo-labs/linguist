# L1 — 01-mental-model.md — Wave 1 Summary

## Edits applied
- **Frontmatter:** added `course-revision: 2026-05-05`.
- **F01-1 (Nit):** bumped `Q3 2025` / `Q4 2025` cohort dates in Skill 1 Example 2 to `Q3 2026` / `Q4 2026`; bumped `"Stripe employees 2025"` query in Skill 3 Example 3 to `2026`.
- **F01-2 (Update):** expanded Skill 3 framing — reworded "Why it matters" to flag that the tool surface is broader than built-ins; added a new ~half-page "The 2026 tool surface" callout listing the five tool *kinds* (built-in, MCP with `alwaysLoad`, Skills with proactive activation via `SKILL.md`, deferred tools with `context: fork`, sub-agent-as-tool with `mcpServers` / `CLAUDE_CODE_FORK_SUBAGENT`). Capability descriptions date-stamped "as of v2.1.128, 2026-05-05" / "v2.1.126, 2026-05-05". Added a tail note on Skill 3 Example 3 explaining the `context: fork` requirement when WebSearch is invoked from inside a Skill.
- **F01-3 (Nit):** removed the unsourced latency / token figures ("800 tokens / 40 seconds", "80 tokens / 3 seconds") from Skill 3 Example 1; replaced with qualitative phrasing ("a long chain of tool calls when one would have done" / "one tool call, deterministic path"). Same point, no fabricated numbers, and not undermined by automatic prompt caching.
- **Tone:** "Agent tool" capital-A framing softened to "sub-agent" / "sub-agents" in two spots (Skill 3 "Why it matters" and Example 3 WITHOUT) since the single-named "Agent tool" is dated; not adding L6/L7 scope, just removing the dated noun.

## Findings NOT applied
None from L1's audit list. All four L1 findings (F01-1, F01-2, F01-3, plus the implicit "tools framing" expansion) were addressed.

## Source-path citations stripped or replaced
- **`services/compact/prompt.ts`** (was in Skill 2 "Why it matters" — "the compaction logic visible in `services/compact/prompt.ts` asks the model to summarise … into nine structured sections"): replaced with capability-level description ("Claude Code's compaction logic asks the model to summarise the full conversation into a structured set of sections"). The exact path was not re-verified against v2.1.128, and the "nine sections" count is similarly unverified — both excised rather than guessed. This is L1's only cited harness path; no other path citations existed in the file.

No WebFetch verification was needed because the only path citation was excised rather than retained.
