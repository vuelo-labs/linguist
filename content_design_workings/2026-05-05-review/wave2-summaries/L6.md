# L6 — Tool Fluency (Wave 2 summary)

Rewrote L6 around a tool *taxonomy* + multi-axis cost model rather than the old single Read<Glob<Grep<Bash<Agent hierarchy.

**Edits applied**
- B06-1: New taxonomy table — built-in, MCP (`alwaysLoad`, reconnect-summarisation v2.1.128, per-agent `mcpServers` v2.1.117), Skill (pointer to standalone Skills/Plugins/Hooks module), deferred tools, Advisor (header `advisor-tool-2026-03-01`), sub-agent-as-tool, with computer use + code execution v2 named under built-in. Axes: cost shape, latency, cache impact, permission surface, restartability. Cost hierarchy preserved as a within-row note.
- F06-2: Retired the single-capital "Agent tool" framing; now subagents (frontmatter, `mcpServers`) and forked subagents (`CLAUDE_CODE_FORK_SUBAGENT=1`), pointed at the Agent Design module.
- F06-3: Example 3 rewritten — WebSearch as a deferred tool requiring `context: fork`, with explicit cache-hygiene reasoning.
- F06-4: Added `/usage`, `/skills`, `/mcp`, and a one-line hooks pointer (`mcp_tool`, `PostToolUse` replace, `duration_ms`, SessionStart/PreToolUse) — depth deferred to standalone module.

**Scope honoured:** No Skills, Plugins, Hooks, SKILL.md anatomy, or plugin tour added — only references.

**Conventions:** `course-revision: 2026-05-05` frontmatter; `constants/prompts.ts` cite stamped "as of v2.1.128, 2026-05-05"; Advisor + deferred-tool + Memory references date-stamped.
